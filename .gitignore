__pycache__/
*.pyc
.env
.venv/
venv/
.ipynb_checkpoints/
.DS_Store
data/raw/
*.sqlite
*.db
# Uncompressed trades file — keep only the .gz version in git (144MB raw exceeds GitHub limits)
data/trades.csv
# Report PDFs are local-only — never push to GitHub
report/*.pdf
*.pdf
